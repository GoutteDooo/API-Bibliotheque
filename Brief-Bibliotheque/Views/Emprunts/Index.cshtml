@model IEnumerable<Brief_Bibliotheque.Models.Classes.Emprunts>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateEmprunt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstRendu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RetourEmprunt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdLivres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUtilisateurs)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateEmprunt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstRendu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RetourEmprunt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdLivres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUtilisateurs)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
